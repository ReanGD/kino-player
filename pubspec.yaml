name: kino_player
description: Kino player
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.11.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  better_player: ^0.0.43
  cached_network_image: ^2.5.0
  cupertino_icons: ^1.0.1
  http: ^0.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true

flutter_intl:
  enabled: true
